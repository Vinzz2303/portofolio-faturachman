╔══════════════════════════════════════════════════════════════════╗
║  V1.7 — NEWS FIX + PORTFOLIO XAU FIX — VPS DEPLOY             ║
╚══════════════════════════════════════════════════════════════════╝

MASALAH YANG DIPERBAIKI:
1. News "belum terhubung" → duplicate route di index.ts menangkap request duluan
2. Portfolio "Sebagian data pasar tidak dapat dimuat" → XAU tidak dikenali Yahoo Finance
3. Language leakage di Portfolio page

═══════════════════════════════════════════════════════════════════
FILE YANG HARUS DI-COPY KE VPS (dari lokal → VPS)
═══════════════════════════════════════════════════════════════════

FRONTEND (di-build ulang setelah copy):
  src/pages/ExploreIntelligence.tsx           ← stable NEWS_SYMBOLS ref
  src/pages/Portfolio.tsx                     ← i18n data banner
  src/services/marketData.ts                  ← news debug logging
  src/lib/stockService.ts                     ← XAU → GC=F symbol mapper
  src/utils/explorePortfolioRelevance.ts      ← FILE BARU (copy ke folder ini)
  src/utils/exploreIntelligenceI18n.ts        ← new i18n keys

BACKEND:
  server/src/services/marketExploreService.ts ← AV ticker format + URL fix
  server/src/index.ts                        ← HAPUS duplicate routes line 2525-2608

═══════════════════════════════════════════════════════════════════
LANGKAH DI VPS
═══════════════════════════════════════════════════════════════════

1) Copy semua file diatas ke VPS (sesuai path)

2) Build frontend:
   cd C:\inetpub\wwwroot\portofolio-faturachman
   npm run build
   
3) Copy dist ke webroot:
   Copy-Item -Path ".\dist\*" -Destination "C:\inetpub\wwwroot" -Recurse -Force

4) Restart backend:
   cd server
   pm2 restart ting-ai-backend --update-env
   pm2 save

5) Test:
   - Buka https://faturachman.my.id/api/market/news?symbols=BTC-USD,GC=F,SPY
     → Harus return ok:true dengan data berita
   - Buka Explore Intelligence → News harus muncul
   - Buka Portfolio workspace → Banner merah harus hilang

═══════════════════════════════════════════════════════════════════
PENTING: server/src/index.ts
═══════════════════════════════════════════════════════════════════

File ini HARUS di-copy LENGKAP karena ada duplicate routes yang dihapus.
Jangan hanya edit sebagian — copy file utuh dari lokal.

Cek setelah deploy:
  - Route /api/market/news hanya ada 1x (di bagian bawah file, ~line 4085)
  - TIDAK ADA lagi route /api/market/news di sekitar line 2583
  - TIDAK ADA lagi route /api/market/quote di sekitar line 2525
  - TIDAK ADA lagi route /api/market/history di sekitar line 2557
